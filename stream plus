<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>StreamPlus – Multi-Platform Streaming</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
}

:root {
  --primary: #FFD700;
  --primary-dark: #FFC400;
  --secondary: #1a1a1a;
  --background: #0F0F0F;
  --surface: #1E1E1E;
  --surface-light: #2D2D2D;
  --text: #FFFFFF;
  --text-secondary: #CCCCCC;
  --accent: #FFA500;
  --radius: 12px;
  --shadow: 0 8px 30px rgba(255, 215, 0, 0.2);
  --transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  --floating: float 6s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}

@keyframes flyIn {
  from { 
    opacity: 0;
    transform: translateY(30px) scale(0.95);
  }
  to { 
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes glow {
  0%, 100% { box-shadow: 0 0 20px rgba(255, 215, 0, 0.4); }
  50% { box-shadow: 0 0 30px rgba(255, 215, 0, 0.7); }
}

@keyframes slideInRight {
  from { transform: translateX(100%); opacity: 0; }
  to { transform: translateX(0); opacity: 1; }
}

@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.05); }
  100% { transform: scale(1); }
}

@keyframes searchGlow {
  0%, 100% { box-shadow: 0 0 20px rgba(255, 215, 0, 0.2); }
  50% { box-shadow: 0 0 30px rgba(255, 215, 0, 0.4); }
}

body {
  background: linear-gradient(135deg, #0A0A0A 0%, #1A1A1A 100%);
  color: var(--text);
  min-height: 100vh;
  overflow-x: hidden;
  background-attachment: fixed;
}

/* Header - Reorganized Layout */
header {
  background: linear-gradient(180deg, rgba(30, 30, 30, 0.98) 0%, rgba(30, 30, 30, 0.95) 100%);
  position: sticky;
  top: 0;
  z-index: 1000;
  backdrop-filter: blur(20px);
  border-bottom: 2px solid var(--primary);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
}

/* Top Header Row */
.header-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 32px;
  border-bottom: 1px solid rgba(255, 215, 0, 0.1);
}

.logo {
  color: var(--primary);
  font-size: 28px;
  font-weight: 800;
  letter-spacing: -0.5px;
  display: flex;
  align-items: center;
  gap: 12px;
  white-space: nowrap;
  animation: glow 4s infinite;
  padding: 8px 16px;
  border-radius: var(--radius);
  background: rgba(255, 215, 0, 0.1);
}

.logo i {
  font-size: 24px;
  animation: pulse 2s infinite;
}

/* User Profile in Top Row */
.user-profile {
  display: flex;
  align-items: center;
  gap: 15px;
  flex-shrink: 0;
}

.profile-btn {
  background: linear-gradient(135deg, var(--primary), var(--accent));
  border: none;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #000;
  cursor: pointer;
  transition: var(--transition);
  animation: float 3s ease-in-out infinite;
  box-shadow: 0 5px 20px rgba(255, 215, 0, 0.3);
  font-weight: 700;
  font-size: 18px;
}

.profile-btn:hover {
  transform: scale(1.1) rotate(15deg);
  animation-play-state: paused;
}

/* Middle Header Row - Search Bar */
.header-middle {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 16px 32px;
  position: relative;
}

/* Enhanced Search Bar */
.search-container {
  position: relative;
  width: 100%;
  max-width: 800px;
  animation: float 4s ease-in-out infinite;
}

.search-wrapper {
  position: relative;
  background: rgba(255, 255, 255, 0.08);
  border: 2px solid rgba(255, 215, 0, 0.3);
  border-radius: 30px;
  padding: 8px;
  transition: var(--transition);
  backdrop-filter: blur(10px);
  animation: searchGlow 3s ease-in-out infinite;
}

.search-wrapper:focus-within {
  border-color: var(--primary);
  background: rgba(255, 215, 0, 0.1);
  box-shadow: 0 0 40px rgba(255, 215, 0, 0.3);
  transform: scale(1.02);
  animation: none;
}

.search {
  width: 100%;
  background: transparent;
  border: none;
  color: var(--text);
  padding: 14px 20px 14px 50px;
  font-size: 16px;
  transition: var(--transition);
  outline: none;
}

.search::placeholder {
  color: rgba(255, 255, 255, 0.5);
  font-weight: 300;
  font-size: 15px;
}

.search-container i {
  position: absolute;
  left: 25px;
  top: 50%;
  transform: translateY(-50%);
  color: var(--primary);
  font-size: 18px;
  z-index: 2;
}

.search-results {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  background: var(--surface);
  border-radius: var(--radius);
  margin-top: 10px;
  box-shadow: var(--shadow);
  display: none;
  max-height: 400px;
  overflow-y: auto;
  z-index: 1001;
  border: 1px solid rgba(255, 215, 0, 0.2);
  animation: flyIn 0.3s ease;
}

.search-result-item {
  padding: 12px 20px;
  display: flex;
  align-items: center;
  gap: 15px;
  cursor: pointer;
  transition: var(--transition);
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.search-result-item:hover {
  background: rgba(255, 215, 0, 0.1);
}

.search-result-item img {
  width: 60px;
  height: 90px;
  object-fit: cover;
  border-radius: 8px;
}

.search-result-info h4 {
  font-size: 15px;
  font-weight: 600;
  margin-bottom: 4px;
}

.search-result-info p {
  font-size: 13px;
  color: var(--text-secondary);
}

/* Bottom Header Row - Categories */
.header-bottom {
  padding: 8px 32px;
  background: rgba(20, 20, 20, 0.8);
  border-top: 1px solid rgba(255, 215, 0, 0.1);
  border-bottom: 1px solid rgba(255, 215, 0, 0.1);
}

/* Category Navigation - Below Search Bar */
.category-nav-container {
  width: 100%;
  overflow-x: auto;
  overflow-y: hidden;
  white-space: nowrap;
  scrollbar-width: thin;
  scrollbar-color: var(--primary) transparent;
  padding: 5px 0;
}

.category-nav-container::-webkit-scrollbar {
  height: 6px;
}

.category-nav-container::-webkit-scrollbar-track {
  background: transparent;
  border-radius: 10px;
}

.category-nav-container::-webkit-scrollbar-thumb {
  background: var(--primary);
  border-radius: 10px;
}

.category-nav-container::-webkit-scrollbar-thumb:hover {
  background: var(--primary-dark);
}

.category-nav {
  display: inline-flex;
  gap: 10px;
  padding: 5px 10px;
}

.nav-btn {
  background: rgba(255, 215, 0, 0.08);
  border: 2px solid rgba(255, 215, 0, 0.15);
  color: var(--text-secondary);
  padding: 10px 22px;
  cursor: pointer;
  border-radius: 25px;
  font-size: 14px;
  font-weight: 500;
  transition: var(--transition);
  display: flex;
  align-items: center;
  gap: 8px;
  white-space: nowrap;
  flex-shrink: 0;
  backdrop-filter: blur(10px);
  animation: float 6s ease-in-out infinite;
  animation-delay: calc(var(--i) * 0.2s);
}

.nav-btn:hover {
  background: rgba(255, 215, 0, 0.2);
  border-color: var(--primary);
  color: var(--text);
  transform: translateY(-4px);
  box-shadow: 0 5px 20px rgba(255, 215, 0, 0.2);
}

.nav-btn.active {
  background: linear-gradient(135deg, var(--primary), var(--accent));
  color: #000;
  font-weight: 700;
  border-color: transparent;
  animation: pulse 2s infinite, float 4s ease-in-out infinite;
  box-shadow: 0 5px 25px rgba(255, 215, 0, 0.4);
}

.nav-btn i {
  font-size: 15px;
}

/* Main Content */
main {
  padding: 32px;
  max-width: 1800px;
  margin: 0 auto;
  animation: flyIn 0.8s ease-out;
}

.section {
  margin-bottom: 48px;
  animation: slideInRight 0.6s ease-out;
}

.section:nth-child(even) {
  animation-delay: 0.2s;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 28px;
}

.section-title {
  font-size: 26px;
  font-weight: 700;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.view-all {
  color: var(--primary);
  background: none;
  border: 2px solid var(--primary);
  padding: 10px 24px;
  border-radius: 25px;
  font-size: 14px;
  cursor: pointer;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: var(--transition);
  backdrop-filter: blur(10px);
}

.view-all:hover {
  background: rgba(255, 215, 0, 0.1);
  transform: translateX(5px);
  gap: 12px;
}

/* Cards Grid */
.row {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: 24px;
  margin-bottom: 16px;
}

.card {
  background: linear-gradient(145deg, var(--surface), #252525);
  border-radius: var(--radius);
  overflow: hidden;
  cursor: pointer;
  transition: var(--transition);
  position: relative;
  border: 1px solid rgba(255, 215, 0, 0.1);
  animation: float 6s ease-in-out infinite;
  animation-delay: calc(var(--i) * 0.1s);
}

.card:hover {
  transform: translateY(-12px) scale(1.03);
  box-shadow: var(--shadow), 0 20px 40px rgba(0, 0, 0, 0.4);
  border-color: var(--primary);
  animation-play-state: paused;
}

.card-image {
  width: 100%;
  aspect-ratio: 2/3;
  object-fit: cover;
  display: block;
  transition: var(--transition);
}

.card:hover .card-image {
  transform: scale(1.05);
}

.card-content {
  padding: 16px;
}

.card-title {
  font-size: 16px;
  font-weight: 600;
  margin-bottom: 6px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.card-meta {
  font-size: 13px;
  color: var(--text-secondary);
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.card-category {
  color: var(--primary);
  font-weight: 500;
  padding: 4px 10px;
  background: rgba(255, 215, 0, 0.1);
  border-radius: 12px;
  font-size: 12px;
}

/* Card Stats */
.card-stats {
  display: flex;
  gap: 15px;
  margin-top: 10px;
  padding-top: 10px;
  border-top: 1px solid rgba(255, 255, 255, 0.05);
}

.card-stat {
  display: flex;
  align-items: center;
  gap: 5px;
  font-size: 12px;
  color: var(--text-secondary);
}

.card-stat i {
  color: var(--primary);
  font-size: 11px;
}

/* Premium Badge */
.premium-badge {
  position: absolute;
  top: 12px;
  left: 12px;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  color: #000;
  font-size: 11px;
  font-weight: 800;
  padding: 6px 12px;
  border-radius: 20px;
  display: flex;
  align-items: center;
  gap: 6px;
  z-index: 2;
  animation: pulse 2s infinite;
  box-shadow: 0 4px 15px rgba(255, 215, 0, 0.4);
}

/* Enhanced Video Player */
.player {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.95);
  display: none;
  align-items: center;
  justify-content: center;
  z-index: 2000;
  animation: flyIn 0.5s ease-out;
  backdrop-filter: blur(20px);
}

.player-content {
  width: 90%;
  max-width: 1200px;
  background: var(--surface);
  border-radius: var(--radius);
  overflow: hidden;
  box-shadow: var(--shadow), 0 0 60px rgba(255, 215, 0, 0.3);
}

.player-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 32px;
  background: rgba(0, 0, 0, 0.8);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid rgba(255, 215, 0, 0.2);
}

.player-title {
  font-size: 20px;
  font-weight: 600;
  color: var(--primary);
}

.player-stats {
  display: flex;
  gap: 25px;
  align-items: center;
}

.stat {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 14px;
  color: var(--text-secondary);
  cursor: pointer;
  transition: var(--transition);
  padding: 8px 16px;
  border-radius: 20px;
  background: rgba(255, 255, 255, 0.05);
}

.stat:hover {
  background: rgba(255, 215, 0, 0.1);
  color: var(--primary);
}

.stat i {
  font-size: 16px;
}

.close-player {
  background: rgba(255, 215, 0, 0.2);
  border: none;
  color: var(--primary);
  font-size: 24px;
  cursor: pointer;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: var(--transition);
}

.close-player:hover {
  background: var(--primary);
  color: #000;
  transform: rotate(90deg);
}

.video-container {
  position: relative;
}

.player video {
  width: 100%;
  display: block;
  height: 65vh;
  object-fit: contain;
  background: #000;
}

/* Share Panel */
.share-panel {
  position: absolute;
  bottom: 20px;
  right: 20px;
  background: rgba(30, 30, 30, 0.95);
  border-radius: var(--radius);
  padding: 20px;
  box-shadow: var(--shadow);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 215, 0, 0.2);
  display: none;
  z-index: 10;
  animation: flyIn 0.3s ease-out;
  min-width: 300px;
}

.share-panel h4 {
  color: var(--primary);
  margin-bottom: 15px;
  font-size: 16px;
  font-weight: 600;
}

.share-options {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
}

.share-btn {
  background: rgba(255, 255, 255, 0.05);
  border: none;
  border-radius: 10px;
  padding: 12px;
  cursor: pointer;
  transition: var(--transition);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  color: var(--text);
}

.share-btn:hover {
  background: rgba(255, 215, 0, 0.15);
  transform: translateY(-4px);
}

.share-btn i {
  font-size: 20px;
}

.share-btn.facebook { color: #1877F2; }
.share-btn.twitter { color: #1DA1F2; }
.share-btn.whatsapp { color: #25D366; }
.share-btn.telegram { color: #0088CC; }
.share-btn.instagram { color: #E4405F; }
.share-btn.copy { color: var(--primary); }

.share-btn span {
  font-size: 11px;
  font-weight: 500;
}

.share-link {
  margin-top: 15px;
  padding: 10px;
  background: rgba(0, 0, 0, 0.3);
  border-radius: 8px;
  display: flex;
  gap: 10px;
}

.share-link input {
  flex: 1;
  background: transparent;
  border: 1px solid rgba(255, 215, 0, 0.3);
  border-radius: 6px;
  padding: 8px 12px;
  color: var(--text);
  font-size: 13px;
}

.share-link button {
  background: var(--primary);
  color: #000;
  border: none;
  padding: 8px 16px;
  border-radius: 6px;
  cursor: pointer;
  font-weight: 600;
  font-size: 13px;
  transition: var(--transition);
}

.share-link button:hover {
  background: var(--primary-dark);
}

/* Player Controls */
.player-controls {
  background: rgba(0, 0, 0, 0.8);
  padding: 20px 32px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-top: 1px solid rgba(255, 215, 0, 0.2);
}

.control-buttons {
  display: flex;
  gap: 15px;
}

.control-btn {
  background: rgba(255, 215, 0, 0.1);
  border: 1px solid rgba(255, 215, 0, 0.3);
  color: var(--primary);
  padding: 10px 20px;
  border-radius: 25px;
  cursor: pointer;
  font-size: 14px;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: var(--transition);
}

.control-btn:hover {
  background: rgba(255, 215, 0, 0.2);
  transform: translateY(-3px);
}

/* Footer */
footer {
  text-align: center;
  padding: 40px 24px;
  color: var(--text-secondary);
  font-size: 14px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  margin-top: 60px;
  background: rgba(15, 15, 15, 0.8);
  backdrop-filter: blur(10px);
}

footer p {
  margin-bottom: 12px;
}

.footer-links {
  display: flex;
  justify-content: center;
  gap: 24px;
  margin-top: 20px;
  flex-wrap: wrap;
}

.footer-links a {
  color: var(--text-secondary);
  text-decoration: none;
  transition: var(--transition);
  padding: 8px 16px;
  border-radius: 20px;
}

.footer-links a:hover {
  color: var(--primary);
  background: rgba(255, 215, 0, 0.1);
  transform: translateY(-2px);
}

/* Responsive Design */
@media (max-width: 1024px) {
  .header-top,
  .header-middle,
  .header-bottom {
    padding: 12px 20px;
  }
  
  .search-container {
    max-width: 600px;
  }
}

@media (max-width: 768px) {
  .row {
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 16px;
  }
  
  main {
    padding: 20px;
  }
  
  .section-title {
    font-size: 22px;
  }
  
  .share-options {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .nav-btn {
    padding: 8px 16px;
    font-size: 13px;
  }
  
  .logo {
    font-size: 22px;
  }
}

@media (max-width: 480px) {
  .header-top,
  .header-middle,
  .header-bottom {
    padding: 10px 16px;
  }
  
  .logo {
    font-size: 20px;
    padding: 6px 12px;
  }
  
  .nav-btn {
    padding: 6px 12px;
    font-size: 12px;
  }
  
  .profile-btn {
    width: 40px;
    height: 40px;
    font-size: 16px;
  }
  
  .search {
    padding: 12px 20px 12px 40px;
    font-size: 14px;
  }
  
  .search-container i {
    left: 20px;
    font-size: 16px;
  }
}
</style>
</head>
<body>
  <!-- Header with 3 Rows -->
  <header>
    <!-- Top Row: Logo and User Profile -->
    <div class="header-top">
      <div class="logo">
        <i class="fas fa-play-circle"></i>
        <span>StreamPlus</span>
      </div>
      
      <div class="user-profile">
        <button class="profile-btn" onclick="togglePremium()" title="Toggle Premium Status">
          <i id="premiumIcon" class="fas fa-crown"></i>
        </button>
      </div>
    </div>
    
    <!-- Middle Row: Search Bar -->
    <div class="header-middle">
      <div class="search-container">
        <div class="search-wrapper">
          <i class="fas fa-search"></i>
          <input class="search" id="search" placeholder="Search movies, shows, reels..." 
                 oninput="searchMovie()" onfocus="showSearchResults()">
        </div>
        <div class="search-results" id="searchResults"></div>
      </div>
    </div>
    
    <!-- Bottom Row: Categories -->
    <div class="header-bottom">
      <div class="category-nav-container">
        <div class="category-nav">
          <button class="nav-btn active" onclick="filterCat('all', this)" style="--i:0">
            <i class="fas fa-home"></i>
            <span>Home</span>
          </button>
          <button class="nav-btn" onclick="filterCat('action', this)" style="--i:1">
            <i class="fas fa-explosion"></i>
            <span>Action</span>
          </button>
          <button class="nav-btn" onclick="filterCat('comedy', this)" style="--i:2">
            <i class="fas fa-laugh"></i>
            <span>Comedy</span>
          </button>
          <button class="nav-btn" onclick="filterCat('drama', this)" style="--i:3">
            <i class="fas fa-masks-theater"></i>
            <span>Drama</span>
          </button>
          <button class="nav-btn" onclick="filterCat('sci-fi', this)" style="--i:4">
            <i class="fas fa-rocket"></i>
            <span>Sci-Fi</span>
          </button>
          <button class="nav-btn" onclick="filterCat('horror', this)" style="--i:5">
            <i class="fas fa-ghost"></i>
            <span>Horror</span>
          </button>
          <button class="nav-btn" onclick="filterCat('romance', this)" style="--i:6">
            <i class="fas fa-heart"></i>
            <span>Romance</span>
          </button>
          <button class="nav-btn" onclick="filterCat('animation', this)" style="--i:7">
            <i class="fas fa-film"></i>
            <span>Animation</span>
          </button>
          <button class="nav-btn" onclick="filterCat('premium', this)" style="--i:8">
            <i class="fas fa-crown"></i>
            <span>Premium</span>
          </button>
          <button class="nav-btn" onclick="filterCat('reels', this)" style="--i:9">
            <i class="fas fa-play"></i>
            <span>Reels</span>
          </button>
        </div>
      </div>
    </div>
  </header>
  
  <!-- Main Content -->
  <main>
    <div class="section" id="moviesSection">
      <div class="section-header">
        <h2 class="section-title">🎬 Trending Now</h2>
        <button class="view-all" onclick="filterCat('all', document.querySelector('.nav-btn.active'))">
          View All <i class="fas fa-arrow-right"></i>
        </button>
      </div>
      
      <div class="row" id="movieList"></div>
    </div>
    
    <div class="section" id="reelsSection">
      <div class="section-header">
        <h2 class="section-title">📱 Short Videos</h2>
        <button class="view-all" onclick="filterCat('reels', this)">
          View All <i class="fas fa-arrow-right"></i>
          
